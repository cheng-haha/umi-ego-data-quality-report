# UMI / Ego 数据质量审计结果

审计日期：2026-07-29  
范围：4 个数据组、6 段录制  
结论类型：检查数据能否读取和整理，不能代替真实机器人测试

## 先看最重要的结论

- **视频能看，文件没有明显损坏。** 6 段视频都能从头到尾正常读取。
- **运动轨迹基本正常。** 没有发现时间倒退、非法数值或明显跳变。
- **FastUMI 轨迹可以用，但交付材料少一份参考点说明。** 需要供应方确认
  `merged_trajectory` 是否已经是夹爪中心（TCP）；如果不是，补一份
  `T_pose→TCP` 固定变换即可。
- **Ego 的 3D 手点能读，但供应方没有说清楚怎样投到 RGB。** 旧画法会
  明显偏；新版目测校正后中位偏差约 8–10 像素。它适合人工检查，不是厂家
  标定，另外约 15% 的画面仍缺少及时手点。
- **没有任务文字。** 数据没有说明每段视频在做什么、做成功没有。
- **真机部署不作为这批数据的扣分项。** 机器人模型、控制接口、限速和碰撞
  检查是任何示教数据上机时都要做的后续工作。

因此，这批数据可以用于离线研究、轨迹整理和固定任务实验。训练语言条件
VLA 前需要补任务文字；真机执行属于后续部署工程，不用于判断这批数据好坏。

## 我目前的直观判断（方便沟通）

- **Ego 数据整体质量比较一般，主要有下面几个问题：**
  - 大约 15% 的视频帧没有及时对应上的手部数据，不能直接当作可靠标签。
  - 部分右手数据存在重复帧，遮挡时还会出现手部骨骼明显变形，需要过滤。
  - 原始骨骼投影有明显偏移，说明镜头模型、坐标系或者外参的使用方法还没有
    完全确认。我已经把网页里的投影调整到基本能对上，但这只是方便肉眼检查，
    不能当成厂家提供的精确标定结果。
  - 带 ToF 的数据虽然能正常读取深度图，但 RGB 和深度是否完成了准确对齐，
    最好再让供应方确认。
  - Ego 记录的是人的头戴视角和手部骨骼，并不是机器人可以直接执行的
    EEF/TCP 轨迹；如果用于机器人训练，后面还需要做动作映射或重定向。
- **FastUMI 新版本质量相对不错。** 视频、相机内参和运动轨迹基本可用，
  只需要删除极少量重复 IMU 记录，并让供应方确认 pose 对应的具体参考点。
- **FastUMI 旧版本可以先不考虑。** 本次交付缺少每台设备自己的相机内参，
  也没有 IMU；除非后面能补齐，否则没有必要优先处理。
- **目前还没看到长程任务，也没看到包含大范围移动的 FastUMI 数据。**
  现有样例更像短时、固定区域内的操作，能不能覆盖我们的使用场景还需要
  跟供应方确认。
- **大部分常规问题可以通过清洗解决。** 例如异常帧、重复记录、格式不统一
  和轻微同步偏差，都可以在转换时处理；但缺失的标定说明和任务标签不能靠
  清洗凭空补出来。
- **当前最关键的问题是没有文本任务标注。** 需要先问供应方有没有对应的
  任务指令、动作阶段和成功/失败标签。如果确实没有，就只能先用 VLM 自动
  生成一版，再通过人工抽检和修正来保证质量。

| 数据组 | 录制 | 检查分 | 通俗结论 |
|---|---|---:|---|
| ego不带tof | ego不带tof | 82 | 视频和 3D 手点能读；旧投影会偏，新版目测校正后约 10.2 像素，但供应方仍需说明坐标系 |
| ego带tof | ego带tof | 85 | 视频、深度和 3D 手点能读；新版目测校正后约 8.3 像素，右手异常帧仍要过滤 |
| fastumi旧数据 | 左手 | 97 | 视频和轨迹能用；本次交付包未提供这台设备自己的相机内参文件，也未提供 IMU |
| fastumi旧数据 | 右手 | 94 | 与左手相同；夹爪开度长时间不变，需要对照视频确认是正常保持还是信号卡住 |
| fastumi新 | 左手 | 94 | 四批里最完整；删除 1 条重复 IMU 时间记录后可开始转换 |
| fastumi新 | 右手 | 94 | 四批里最完整；删除 2 条重复 IMU 时间记录后可开始转换 |

检查分只表示自动检查发现的问题多少，不代表训练价值或机器人成功率。

## 哪些地方没有明显问题

- 6 段 RGB 视频都是 1280×1280、60 帧/秒，视频帧和时间戳数量完全一致。
- 抽查的 809 帧里没有黑屏或明显缺少细节的画面。两段新 FastUMI 只有很短
  的静止片段，最长 0.6 秒。
- 所有运动轨迹都能读取，没有发现时间倒退、非法旋转、越界位置或明显跳点。
- 4 段 FastUMI 融合轨迹都完整，没有跳过数据或匹配失败。
- FastUMI 视频与融合轨迹的时间差约为 5 毫秒，整体对时良好。
- 带 ToF 的 Ego 中，720 张深度图都存在；抽查文件都能读取，约 95.5%
  的像素有有效深度。
- Ego 左右手的 21 个关节文件都齐全；新版目测校正后的骨架大部分时间能
  跟到手的位置。

## 购买或训练前要处理什么

### 1. 有些画面找不到可靠的手部位置

- 两段 Ego 中，大约 83%–85% 的视频帧能在 25 毫秒内找到对应手部数据。
  换句话说，大约 15% 左右的画面不能直接当作可靠手部标签。
- 最长断档约 100–233 毫秒；两段右手数据分别有 84 和 252 个重复帧编号。
- 带 ToF 的 Ego 右手在部分遮挡帧中明显变形。

处理方法：按真实时间对齐，为每帧保存“是否有效”的标记；超过 50 毫秒的
缺口不要强行补点，明显变形的手部帧应删除或降低训练权重。

### 2. Ego 偏移主要是投影方式不匹配，不是简单把外参用反了

- 旧预览直接用 RGB 相机的 SEUCM/EUCM 参数画 3D 手点。两套 Ego 数据
  都显示纵向焦距不匹配，所以骨架主要在上下方向偏。
- 我们分别试了现有 `extrinsic.json` 的正向和反向，两套数据的结果不一致，
  不能证明它就是“手部 3D 点到 RGB”的外参。
- 使用另一半帧检查后，带 ToF 数据的中位偏差从约 48.6 像素降到 8.3
  像素；不带 ToF 从约 24.3 像素降到 10.2 像素。带 ToF 还做了 +20 毫秒
  的小幅时间修正。
- 这套“手部虚拟针孔”只为目测叠图服务，不能当相机的物理标定，也不能
  直接作为毫米级训练真值。
- 两段旧 FastUMI 的本次交付包都未提供各自设备的相机内参文件。预览暂时
  借用了其他设备参数的中间值，并标有 `BORROWED median intrinsic`。
- `ego不带tof` 的相机外参指向了不存在的 RGBD 数据流，供应方应修正。

处理方法：要求供应方明确手点属于哪个坐标系、使用哪套投影模型，以及
`extrinsic.json` 两端分别是什么；最好再提供官方 SDK 重投影或标定板误差。
旧 FastUMI 还需要供应方补充每台设备自己的相机内参文件。

### 3. FastUMI 轨迹能用，但购买前应补一份参考点说明

- `merged_trajectory` 只说明它是 SLAM 与 VIVE 融合后的运动轨迹，没有
  说明轨迹原点在相机、夹爪中心还是其他位置。
- 如果供应方确认这个文件已经完成了设备到夹爪中心的转换，它就可以直接
  按 TCP pose 使用；如果尚未转换，只需再提供 `T_pose→TCP` 固定变换。
- 当前 HTML 中 FastUMI 视频已画红 X、绿 Y、蓝 Z 右手坐标系以及未来
  终点坐标系。默认使用 `pose→TCP = (0, 0.13, 0.10) m`、零 RPY，并明确
  标成“假设值”。它只是为了让人检查方向，不能当成真实标定结果。

处理方法：购买前让供应方回答一句“`merged_trajectory` 的原点是什么”，
并提供已经使用或仍需应用的 4×4 `T_pose→TCP`。这是交付说明缺口，不等于
运动轨迹本身不能用。

### 4. 少量 IMU 重复，以及一段夹爪信号长时间不变

- 新 FastUMI 左右手分别只有 1 条和 2 条重复 IMU 记录，占比非常小，
  转换副本时删除即可，不要修改原始压缩包。
- 旧 FastUMI 没有 IMU。模型不使用 IMU 时不影响，否则会缺少输入。
- 旧 FastUMI 右手有 58.2% 的相邻夹爪数值完全相同。这可能只是操作者
  一直保持夹爪不动，也可能是信号卡住，需要对照视频确认。

### 5. 只有“录像和运动记录”，没有“任务说明书”

- 4 个压缩包和解压文件里都没有说明每段视频在做什么、动作分几步，以及
  最后是否成功。
- TXT/CSV/JSON 虽然有文字，但只是字段名、关节名、时间戳、相机参数和
  轨迹统计，不是“拿起杯子放到桌上”这样的任务描述。
- 7 个原始视频也没有声音或字幕，无法从操作者语音恢复任务。

处理方法：先向供应方索要原始任务清单、每段任务起止位置和成功/失败记录。
如果确实没有，可以让 VLM 先看视频写任务描述、动作步骤和结果，再由人工
逐条检查。VLM 生成的内容只能叫“后补标签”，不能冒充采集时的原始真值。

## 最终使用建议

1. **FastUMI 新数据：** 删除 1–2 条重复 IMU 记录即可开始整理；同时让
   供应方确认 pose 是否已经是 TCP，或补一份 `T_pose→TCP`。
2. **FastUMI 旧数据：** 视频和运动轨迹可以用；在找回每台设备自己的
   相机参数前，不要用它训练精确的三维到二维投影。
3. **Ego 数据：** 视频、SLAM、IMU 和 3D 手点可以继续整理；手点必须带
   “是否有效”标记。新版投影可用来人工验收，但在供应方说明坐标系或做
   标定板验证前，不要把它当成精确 RGB 标注。
4. **语言条件 VLA：** 先补任务文字。VLM 可以批量打草稿，但任务名称、
   关键物体、动作阶段和成功/失败结果必须人工复核。
5. **真实机器人执行：** 机器人模型、控制接口、限速、碰撞检查和低速空跑
   属于通用部署流程，不作为本批数据的质量扣分项。

详细数值见 `quality_report.html`、`quality_report.json` 和
`metrics.csv`。人工检查可直接播放 `previews/*.mp4`。
